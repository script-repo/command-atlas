{
  "name": "command-atlas-terminal",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-user backend that serves the Command Atlas and gives its terminal deck real, per-user shells authenticated against local Linux accounts (PAM).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "os": [
    "linux"
  ],
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0",
    "authenticate-pam": "^1.0.5",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  }
}
